from setuptools import setup

setup(name='main.py',
      version='0.1',
      description='Flask set up for the backend',
      url='https://github.com/ratemybronco/backend-testing.git',
      packages=['backend-testing'])